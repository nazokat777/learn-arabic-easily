name: Deploy to Vercel

# Har `main` branchga push bo'lganda Flutter web quriladi va Vercel'ga yuklanadi.
on:
  push:
    branches: [main]
  workflow_dispatch: {} # qo'lda ham ishga tushirish mumkin

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: app
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.41.9
          channel: stable
          cache: true

      - name: Dependencies
        run: flutter pub get

      - name: Build web (release)
        run: flutter build web --release

      - name: Install Vercel CLI
        run: npm install -g vercel@latest

      - name: Deploy to Vercel (production)
        working-directory: app/build/web
        # --archive=tgz: fayllarni tgz arxiv qilib yuklaydi (CI'da tez, bittalab
        # yuklashda qotib qolmaydi).
        run: vercel deploy --prod --yes --archive=tgz --token="$VERCEL_TOKEN"
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
